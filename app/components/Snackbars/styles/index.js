'use strict';

import { variables, mixins } from '../../../styles/js';

export const styles = theme => ({
  margin: {
    margin: theme.spacing.unit
  },
  root: {
    top: 10,
    right: 15,
    left: `unset`
  }
});
