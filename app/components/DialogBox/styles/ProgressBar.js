'use strict';

import { base } from '../../../styles/js';
const { variables } = base();

export const styles = theme => ({
  root: { flexGrow: 1 },
  dialogContentText: {
    marginBottom: 10
  }
});
