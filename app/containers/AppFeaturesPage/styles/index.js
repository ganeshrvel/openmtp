'use strict';

import { variables, mixins } from '../../../styles/js';

export const styles = theme => ({
  root: {
    textAlign: `left`,
    padding: '0 30px 30px 30px',
    maxWidth: '800px',
    marginRight: 'auto',
    marginLeft: 'auto',
    overflow: 'auto'
  },
  heading: {},
  body: {
    marginTop: 10
  },
  marginDivider: {
    height: 15
  }
});
