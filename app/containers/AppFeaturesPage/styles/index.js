import { variables, mixins } from '../../../styles/js';

export const styles = theme => ({
  root: {
    textAlign: `left`,
    padding: '10px 30px 30px 30px',
    maxWidth: '800px',
    marginRight: 'auto',
    marginLeft: 'auto',
    overflow: 'auto'
  },
  heading: {},
  body: {},
  marginDivider: {
    height: 15
  }
});
