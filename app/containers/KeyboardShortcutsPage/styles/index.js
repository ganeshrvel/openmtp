'use strict';

import { variables, mixins } from '../../../styles/js';

export const styles = theme => ({
  root: {
    textAlign: `left`,
    padding: '0 30px 30px 30px',
    maxWidth: '800px',
    marginRight: 'auto',
    marginLeft: 'auto',
    overflow: 'auto'
  },
  heading: {
    fontWeight: 'bold'
  },
  body: {
    marginTop: 10
  }
});
