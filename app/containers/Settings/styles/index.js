'use strict';

import { variables, mixins } from '../../../styles/js';

export const styles = theme => ({
  margin: {},
  root: {},
  title: {},
  formGroup: {
    paddingTop: 10
  },
  subheading: {
    marginBottom: 5
  },
  switch: {
    height: 30
  },
  block: {
    marginBottom: 20
  }
});
