'use strict';

import { variables, mixins } from '../../../styles/js';

export const styles = theme => ({
  margin: {},
  root: {},
  title: {},
  subheading: {
    marginBottom: 5
  },
  switch: {
    height: 30
  },
  block: {
    marginBottom: 20
  }
});
