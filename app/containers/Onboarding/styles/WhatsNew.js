import { variables, mixins } from '../../../styles/js';

export const styles = theme => ({
  root: {},
  title: {
    fontWeight: `bold`
  },
  nestedPanel: {
    paddingLeft: 16,
    paddingRight: 16
  }
});
