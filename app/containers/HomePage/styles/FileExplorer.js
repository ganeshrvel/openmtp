'use strict';

import { variables, mixins } from '../../../styles/js';

export const styles = theme => ({
  socialMediaShareBtnsWrapper: {
    width: '100%',
    display: 'flex'
  },
  socialMediaShareBtn: {
    height: 22,
    width: `auto`
  }
});
