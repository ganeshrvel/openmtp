'use strict';

import { base } from '../../../styles/js';
const { variables } = base();

export const styles = theme => ({
  root: {
    width: `100%`,
    height: `100%`
  },
  rootBreadcrumbs: {
    width: `100%`,
    height: `100%`
  },
  pathItem: {
    cursor: `pointer`
  }
});
