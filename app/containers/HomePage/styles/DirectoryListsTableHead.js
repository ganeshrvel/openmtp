'use strict';

import { base } from '../../../styles/js';
const { variables } = base();

export const styles = theme => ({
  tableHeadCell: {
    border: `unset`,
    backgroundColor: '#fbfbfb',
    position: 'sticky',
    top: 0,
    zIndex: 10,
  }
});
